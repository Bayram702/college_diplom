# Database Dumps

This folder contains safe database export files for the project.

- `colleges_db_schema_2026-05-09.sql` is a schema-only PostgreSQL dump.

Full data dumps are intentionally not committed because they can contain emails, phone numbers, passport data, password hashes, and password reset codes.
